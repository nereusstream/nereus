# Nereus v0.1 branch freeze record

## Status

The `v0.1` branch at `a14d925da5763f36208f8ddca7bef31f3eb90b0b` preserves the final V1 product line. Its annotated
`v0.1.0` release tag, release artifacts, and final performance evidence remain pending. The historical `v0.1.0`
branch is intentionally retained and was not rewritten by this branch cut.

## Release identity

| Item | Exact value |
| --- | --- |
| V1 product source base | `d599ea40d9c49a1fef4474d60f985d6fa3f366f2` |
| V1 branch freeze commit | `a14d925da5763f36208f8ddca7bef31f3eb90b0b` |
| Release branch | `v0.1` |
| Gradle version | `0.1.0` |
| Annotated tag | Not created; `refs/tags/v0.1.0` remains pending final release validation |
| Historical branch retained | `refs/heads/v0.1.0@6a3f1d1b9c2e4fe005c93aa091858601b2592f6e` |
| Branch freeze timestamp (UTC) | `2026-08-08T11:40:04Z` |

The immutable release identity will be the eventual `refs/tags/v0.1.0^{commit}` target plus the artifact manifest
checksums and image digests. The tag must not be inferred from a moving branch name.

## External source locks

| Repository / component | Full commit | V1 reference |
| --- | --- | --- |
| Nereus Pulsar fork | `11d7ab15291ca4bbc9cc29dedd7878c4e1311ec9` | `nereus-V1` cut from `5.0.0-M1-nereus` |
| Nereus Kafka fork | `76f62f3b83e882105219b6c7687dbde594a8b8a2` | `nereus-V1` cut from `nereus/future9-native-kafka-storage` |
| Apache Kafka fork base | `427b409cf440f745ad6195673d3342f6bd3974d4` | Exact upstream source base verified by the F9 source-lock gate |

The full commit ids are authoritative. Branch names are navigation aids and must not replace source locks in build,
deployment, or evidence manifests.

## Compatibility decisions

- V1 APIs, metadata, object formats, deployment behavior, and fork integrations remain available on `v0.1`.
- `main` moves to `0.2.0-SNAPSHOT` and may delete or replace V1 interfaces without a compatibility facade.
- V2 is a fresh-deployment line. It must use distinct durable format or namespace identities for changed data and
  reject V1 state explicitly instead of interpreting it as V2.
- Product V2 does not imply a repository-wide codec-version rename. Each changed durable contract receives its own
  previously unused version or namespace identity.

## Branch-cut verification

The following gates passed from clean Nereus and source-locked fork commits on 2026-08-08:

| Gate | Result |
| --- | --- |
| `./gradlew verifyReleaseVersion -PreleaseVersion=0.1.0` | PASS |
| `./gradlew checkPulsarSourceLock -PpulsarCheckout=/Users/liusinan/apps/ideaproject/nereusstream/pulsar` | PASS |
| `./gradlew phase9KafkaForkDevelopmentSourceLockCheck -PkafkaForkCheckout=/Users/liusinan/apps/ideaproject/nereusstream/kafka` | PASS |
| `./gradlew assemble spotlessCheck checkstyleMain checkstyleTest :nereus-admin:check` | PASS |

These branch-cut checks do not substitute for freeze-commit CI, source-locked final aggregates, image builds, artifact
checksums, deployment verification, or benchmark evidence required before creating the annotated tag.

## Artifact identities

No Maven release artifact or runtime/admin image is published by this branch cut. The eventual release process must
record immutable coordinates, SHA-256 checksums, image digests, the Nereus commit, and both fork commits.

## Known limitations and follow-up

- Keep the historical `v0.1.0` branch unchanged until its release-only differences and retained benchmark evidence
  have been audited. If an annotated `v0.1.0` tag is later created while that branch exists, use fully qualified refs.
- The paused v0.1.0 benchmark campaign is not a formal PASS. Its final activation, object-store contract, release
  verification, and authoritative workload gates remain separate work.
- No in-place V1-to-V2 metadata, object, or deployment migration is promised.
